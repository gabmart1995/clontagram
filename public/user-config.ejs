<%- include('partials/header', { title })  %> 
<%- include('partials/navbar', { userLogged }) %>
    
    <div class="content-fluid d-flex justify-content-center">
        <div class="card card-login">
            <div class="card-header">
                Configuracion de mi cuenta               
            </div>
            <div class="card-body">
                <form action="" method="post">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                    
                    <% if (errors && errors.general) { %>
                        <div class="row mb-3 text-center">
                            <div class="error-text"><%= errors.general %></div>
                        </div>
                    <% } %>

                    <div class="row mb-3">
                        <label for="name" class="col-sm-4 col-form-label">Name:</label>
                        <div class="col-sm-8">
                            <input type="text" name="name" id="name" class="form-control <%= errors && errors.name ? 'error-field' : ''  %>" />
                            <% if (errors && errors.name) { %>
                                <div class="error-text"><%= errors.name %></div>
                            <% } %>
                        </div>
                    </div>
                    <div class="row mb-3">
                        <label for="surname" class="col-sm-4 col-form-label">Surname:</label>
                        <div class="col-sm-8">
                            <input type="text" name="surname" id="surname" class="form-control <%= errors && errors.name ? 'error-field' : ''  %>" />
                            <% if (errors && errors.surname) { %>
                                <div class="error-text"><%= errors.surname %></div>
                            <% } %>
                        </div>
                    </div>
                    <div class="row mb-3">
                        <label for="nick" class="col-sm-4 col-form-label">Nickname:</label>
                        <div class="col-sm-8">
                            <input type="text" name="nick" id="nick" class="form-control <%= errors && errors.nick ? 'error-field' : ''  %>" />
                            <% if (errors && errors.nick) { %>
                                <div class="error-text"><%= errors.nick %></div>
                            <% } %>
                        </div>
                    </div>
                    <div class="row mb-3">
                        <label for="email" class="col-sm-4 col-form-label">Email:</label>
                        <div class="col-sm-8">
                            <input class="form-control <%= errors && errors.email ? 'error-field' : ''  %>" type="email" name="email" id="email" />
                            <% if (errors && errors.email) { %>
                                <div class="error-text"><%= errors.email %></div>
                            <% } %>
                        </div>
                    </div>
                    <div class="row mb-3">
                        <label for="password" class="col-sm-4 col-form-label">Password:</label>
                        <div class="col-sm-8">
                            <input class="form-control <%= errors && errors.password ? 'error-field' : ''  %>" type="password" name="password" id="password" />
                            <% if (errors && errors.password) { %>
                                <div class="error-text"><%= errors.password %></div>
                            <% } %>
                        </div>
                    </div>
                    <div class="row mb-3">
                        <div class="offset-sm-4 col-sm-8 mt-3">
                            <div class="d-flex flex-row align-items-center">
                                <input type="submit" value="Register" class="btn btn-primary" />
                            </div>
                        </div>
                    </div>

                </form>
            </div>
        </div>
    </div>
<%- include('partials/footer') %>